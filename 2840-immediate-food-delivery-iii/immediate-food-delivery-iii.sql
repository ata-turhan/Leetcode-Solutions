# Write your MySQL query statement below
select order_date, round(100*sum(if(order_date=customer_pref_delivery_date, 1, 0))/count(delivery_id), 2) as immediate_percentage  from delivery group by order_date order by order_date;